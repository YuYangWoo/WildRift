<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.wildlift">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SplashActivity"
            android:label="와일드리프트">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:label = "와일드리프트"/>
        <activity android:name=".champion.ChampionActivity"
          android:windowSoftInputMode="adjustNothing"></activity>
        <activity android:name=".item.ItemActivity"></activity>
        <activity android:name=".rune.RunesActivity"></activity>
        <activity android:name=".champion.ChampionInfo"></activity>
        <activity android:name=".SpellActivity"></activity>
    </application>

</manifest>